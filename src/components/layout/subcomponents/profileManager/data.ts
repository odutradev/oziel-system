import type { SectionItem } from "./types"

export const profileItemsData: SectionItem[] = [
    { name: 'Meu Perfil', path: '/dashboard/profile' },
    { name: 'Meus Logs', path: '/dashboard/logs' },
  ]